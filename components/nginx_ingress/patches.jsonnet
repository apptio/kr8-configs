local helpers = import 'helpers.libsonnet';  // some helper functions

// this standard function processes the helm input and does some standard filtering and outputs the list of objects
helpers.helmInput
